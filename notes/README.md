# Notes

One file per build phase (`phase-0.md`, `phase-1.md`, …), updated after every phase commit. `CLAUDE.md` points here so a fresh session can pick up where the last one stopped.

Each note records: what was built, what was verified (real command output, not claims), decisions taken, open issues, and what the next phase needs.
